{% extends 'base.html.twig' %}

{% block title %}Hello BoutiqueController!{% endblock %}

{% block body %}
<script> 
  
var down = document.getElementById('sidebox-title-v2 h5');
  
        function GFG_Fun() {
            document.getElementsByClassName('alert alert-danger d-block')[0].
            style.visibility = 'hidden';
            down.innerHTML = "Element is hidden";
        }
</script>


 
  
    <section class="vs-product-wrapper vs-product-layout2 space-top newsletter-pb">
      <div class="container">
        <div class="row">
          <div class="col-lg-8">
            <div class="row align-items-center mb-30">
              <div class="col-xl-5">
                <p class="mb-0">Showing 1–24 of 37 item(s)</p>
              </div>
              <div class="col-xl-7 mt-3 mt-xl-0">
                <div class="sort-btn d-flex flex-wrap justify-content-between justify-content-xl-end align-items-start">
                  <select class="form-select form-select-lg" aria-label=".form-select-lg example">
                    <option selected="selected">Open this select menu</option>
                    <option value="1">One</option>
                    <option value="2">Two</option>
                    <option value="3">Three</option>
                  </select>
                  <div class="d-flex mt-3 mt-sm-0">
                    <a href="#" class="icon-btn3 active">
                      <i class="far fa-border-all"></i>
                    </a>
                    <a href="#" class="icon-btn3">
                      <i class="fas fa-list"></i>
                    </a>
                  </div>
                </div>
              </div>
            </div>
            


   <div class="row">
    {% set comp = 0 %}
      {% for produit in produits %}
 
            {% set comp = (comp + produit.stock) %}
            {% if (comp == 0) %}
                   <h1> la boutique est vide pour l'instant</h1>
            {% else %}
                   {% include 'boutique/produit.html.twig' with {produit: produit} only %}
            {% endif %}
    {% endfor %}
   

  
     
           
          </div>
           <div style="margin: auto;
    width: 50%;
    /* border: 3px solid green; */
    padding: 10px;">{{ knp_pagination_render(produits) }}</div>
  </div>
          <div class="col-lg-4">
            <aside class="sidebar-area sticky-top overflow-hidden">
              <div class="widget widget_search">
                {{ form(formSearch) }}
              </div>
              
              <h3 class="sidebox-title-v2 h5">Categories</h3>
              <div class="vs-sidebox-v2">
                <ul class="vs-cat-list1">
                 {% for categorie in categories %}
                 
    
                  <li>
                    <a href=" {{ path('listeproduits',{"id":categorie.id}) }}">{{categorie.nom}}</span>
                    </a>
                  </li>
                  {% endfor %}
                </ul>
              </div>
              
            </aside>
          </div>
        </div>
      </div>
    </section>
{% endblock %}
