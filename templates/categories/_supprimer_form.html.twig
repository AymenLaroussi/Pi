<form method="post" action="{{ path('supprimer_categories', {'id': category.id}) }}" onsubmit="return confirm('vous etes sure de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ category.id) }}">
    <button class="btn">Supprimer</button>
</form>
{{ form_start(form) }}

                                               {{ form_widget(form.ajout, { 'label': 'Ajouter une categorie' }) }}
                                            {{ form_end(form) }}