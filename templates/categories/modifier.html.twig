{% extends 'dashboard.html.twig' %}
{% block body %}
        <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body"><br><br><br><br>
                        <h4 class="card-title"><a href="{{ path('list_categories') }}"><i class="mdi mdi-arrow-left"></i></a>Mettre à jour une catégorie</h4>
                            <div class="form-group">
                                {{ include('categories/_form.html.twig', {'button_label': 'Mise à jour'}) }}
                                {{ include('categories/_supprimer_form.html.twig') }}
                            </div>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
