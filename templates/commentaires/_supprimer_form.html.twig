<form method="post" action="{{ path('supprimer_comment', {'id': comment.id}) }}" onsubmit="return confirm('vous etes sure de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ comment.id) }}">
    <button class="btn">Supprimer</button>
</form>
