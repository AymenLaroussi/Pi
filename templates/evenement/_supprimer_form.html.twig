<form method="post" action="{{ path('supprimer_evenements', {'id': event.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ event.id) }}">
    <button class="btn">Supprimer</button>
</form>
