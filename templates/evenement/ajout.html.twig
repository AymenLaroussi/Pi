{% extends 'dashboard.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <<div class="col-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body"><br><br><br><br>
                    <h4 class="card-title"><a href="{{ path('list_evenements') }}"><i class="mdi mdi-arrow-left"></i></a>  Ajouter un évenement</h4>
                      <div class="form-group">
                           {{ include('evenement/_form.html.twig') }}
                           
                           
                    </div>
                </div>
    </div>
{% endblock %}
