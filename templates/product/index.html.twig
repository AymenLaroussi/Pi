{% extends 'base.html.twig' %}

{% block title %}Les produits
{% endblock %}

{% block body %}
    <br><br><br><br><br><br><br>
    <div class="bg-light p-3">
        <h1>Les produits</h1>
    </div>

    <div class="row">
        {% for product in products %}
            <div class="col-3 mt-3">
                <div class="border border-dark rounded p-2 clearfix">
                    <h2>
                        <h4 style="color: aliceblue">{{ product.title }}</h4>
                        <h4 style="color: aliceblue"> {{ product.price }}DT</h4>
                    </h4>
                    <img class="img-fluid rounded" src="{{ product.image }}" alt="Image de {{ product.title }}">
                    <hr>
                    <a href="{{ path('cart_add', {'id': product.id }) }}" class="btn btn-success float-right">
                        <i class="fas fa-shopping-cart mr-1"></i>
                        Ajouter
                    </a>
                </div>

            </div>
        {% endfor %}
    </div>
    <br><br><br><br><br><br><br>
{% endblock %}
