<form method="post" action="{{ path('supprimer_sponsors', {'id': spons.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ spons.id) }}">
    <button class="btn">Supprimer</button>
</form>
